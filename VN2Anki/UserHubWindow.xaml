<Window x:Class="VN2Anki.UserHubWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="VN2Anki - User Hub" Height="600" Width="900"
        Background="#1E1E1E" Foreground="White" WindowStartupLocation="CenterScreen">

    <Grid>
        <TabControl Background="Transparent" BorderThickness="0" Margin="10">
            <TabItem Header="📚 Library" Foreground="Black">
                <Grid Margin="10">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <Button Grid.Row="0" Content="+ Adicionar Jogo a partir do VNDB" Click="BtnAddVn_Click" Padding="15,8" Background="#007ACC" Foreground="White" FontWeight="Bold" BorderThickness="0" Cursor="Hand" HorizontalAlignment="Left" Margin="0,0,0,15"/>

                    <ListBox Grid.Row="1" ItemsSource="{Binding VisualNovels}" Background="Transparent" BorderThickness="0" ScrollViewer.HorizontalScrollBarVisibility="Disabled">

                        <ListBox.ItemsPanel>
                            <ItemsPanelTemplate>
                                <WrapPanel Orientation="Horizontal" />
                            </ItemsPanelTemplate>
                        </ListBox.ItemsPanel>

                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <Border Background="#2D2D30" CornerRadius="8" Margin="10" Width="200" Height="320" ClipToBounds="True">
                                    <Grid>
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="220"/>
                                            <RowDefinition Height="*"/>
                                        </Grid.RowDefinitions>

                                        <Image Source="{Binding CoverImagePath}" Stretch="UniformToFill" Grid.Row="0"/>
                                        <Border Grid.Row="0" BorderBrush="#3E3E42" BorderThickness="0,0,0,2"/>

                                        <StackPanel Grid.Row="1" Margin="10">
                                            <TextBlock Text="{Binding Title}" Foreground="White" FontWeight="Bold" FontSize="14" TextTrimming="CharacterEllipsis" ToolTip="{Binding Title}"/>
                                            <TextBlock Text="{Binding ProcessName, StringFormat='Processo: {0}'}" Foreground="#AAAAAA" FontSize="11" Margin="0,2,0,8" TextTrimming="CharacterEllipsis"/>

                                            <Grid Margin="0,10,0,0">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="Auto"/>
                                                </Grid.ColumnDefinitions>

                                                <Button Grid.Column="0" Content="▶ Play" Command="{Binding DataContext.PlayVnCommand, RelativeSource={RelativeSource AncestorType=Window}}" CommandParameter="{Binding}" Background="#007ACC" Foreground="White" FontWeight="Bold" BorderThickness="0" Padding="5" Cursor="Hand" Margin="0,0,5,0"/>

                                                <Button Grid.Column="1" Content="🗑" Command="{Binding DataContext.DeleteVnCommand, RelativeSource={RelativeSource AncestorType=Window}}" CommandParameter="{Binding}" Background="#DC3545" Foreground="White" BorderThickness="0" Padding="8,5" Cursor="Hand" ToolTip="Remover da Biblioteca"/>
                                            </Grid>
                                        </StackPanel>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </Grid>
            </TabItem>

            <TabItem Header="⏱️ Sessions History" Foreground="Black">
                <TextBlock Text="Histórico de sessões aparecerá aqui..." Foreground="Gray" HorizontalAlignment="Center" VerticalAlignment="Center"/>
            </TabItem>

            <TabItem Header="📊 Stats" Foreground="Black">
                <TextBlock Text="Gráficos e estatísticas aparecerão aqui..." Foreground="Gray" HorizontalAlignment="Center" VerticalAlignment="Center"/>
            </TabItem>
        </TabControl>
    </Grid>
</Window>