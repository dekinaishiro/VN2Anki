<Window x:Class="VN2Anki.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:loc="clr-namespace:VN2Anki"
        xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
        Title="VN Miner" SizeToContent="WidthAndHeight"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent" Topmost="True" ResizeMode="NoResize" WindowStartupLocation="Manual">

    <Border Background="#333333" CornerRadius="5" BorderBrush="#555" BorderThickness="1" Margin="10">
        <Border.Effect>
            <DropShadowEffect BlurRadius="10" ShadowDepth="2" Opacity="0.5"/>
        </Border.Effect>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0">
                <Border Background="#444" CornerRadius="4,4,0,0" MouseLeftButtonDown="DragBar_MouseLeftButtonDown" Cursor="SizeAll" Height="18"/>
                <TextBlock Text="⋮⋮⋮⋮⋮⋮⋮⋮" Foreground="#888" FontSize="15" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,-3,0,0" IsHitTestVisible="False"/>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                    <Button Content="—" Foreground="#CCC" Background="Transparent" BorderThickness="0" Width="40" Height="18" FontSize="11" Click="BtnMinimize_Click" ToolTip="Minimize" Cursor="Arrow"/>
                    <Button Content="✕" Foreground="#CCC" Background="Transparent" BorderThickness="0" Width="30" Height="18" FontSize="11" Click="BtnExitApp_Click" ToolTip="Shutdown" Cursor="Arrow"/>
                </StackPanel>
            </Grid>

            <Grid x:Name="MiniStatsPanel" Grid.Row="1" Visibility="{Binding MiniStatsVisibility}" Margin="15,10,15,10">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <Grid Grid.Row="0" Grid.ColumnSpan="2" Margin="0,0,0,5">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Grid.Column="0" Text="{Binding DisplayVnTitle}" 
                               Foreground="{Binding VnTitleColor}" 
                               FontWeight="Bold" VerticalAlignment="Center" 
                               TextWrapping="Wrap" MaxHeight="40" MaxWidth="250"
                               TextTrimming="CharacterEllipsis"/>

                    <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                        <Button Command="{Binding SelectVideoCommand}" 
                                Background="#444" BorderThickness="0" 
                                Cursor="Hand" Margin="0,0,4,0" Padding="5"
                                ToolTip="Selecionar fonte de Vídeo">
                            <iconPacks:PackIconMaterial Kind="{Binding VideoIconKind}" Foreground="{Binding VideoIconColor}" Width="16" Height="16"/>
                        </Button>
                        <Button Command="{Binding SelectAudioCommand}" 
                                Background="#444" BorderThickness="0" 
                                Cursor="Hand" Margin="0,0,4,0" Padding="5"
                                ToolTip="Selecionar fonte de Áudio">
                            <iconPacks:PackIconMaterial Kind="{Binding AudioIconKind}" Foreground="{Binding AudioIconColor}" Width="16" Height="16"/>
                        </Button>
                        <Button Command="{Binding ManualLinkActionCommand}" 
                                Background="#444" BorderThickness="0" 
                                Cursor="Hand" Margin="0,0,4,0" Padding="5"
                                ToolTip="Vincular Jogo (Link)">
                            <iconPacks:PackIconMaterial Kind="{Binding LinkIconKind}" Foreground="{Binding LinkIconColor}" Width="16" Height="16"/>
                        </Button>
                    </StackPanel>
                </Grid>

                <StackPanel Grid.Row="1" Grid.Column="0" HorizontalAlignment="Left" VerticalAlignment="Center">
                    <TextBlock Text="{Binding Tracker.Elapsed, StringFormat=hh\\:mm\\:ss}" FontSize="22" FontWeight="Black" Foreground="White" Margin="0,0,0,0"/>
                </StackPanel>

                <StackPanel Grid.Row="1" Grid.Column="1" HorizontalAlignment="Right" VerticalAlignment="Center" Orientation="Horizontal">
                    <TextBlock Text="{Binding Tracker.ValidCharacterCount}" FontSize="22" FontWeight="Black" Foreground="White"/>
                    <TextBlock Text=" char" FontSize="16" Foreground="#AAAAAA" VerticalAlignment="Bottom" Margin="2,0,0,3"/>
                </StackPanel>
            </Grid>

            <TextBlock x:Name="TxtStatus" Grid.Row="2" Foreground="#AAAAAA" FontSize="10" HorizontalAlignment="Center" Margin="5,0,5,2" MaxWidth="250" TextTrimming="CharacterEllipsis" Text="{Binding StatusText}" Visibility="{Binding StatusVisibility}"/>

            <Grid Grid.Row="3" Margin="5">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="30"/>
                    <ColumnDefinition Width="30"/>
                    <ColumnDefinition Width="30"/>
                    <ColumnDefinition Width="30"/>
                    <ColumnDefinition Width="30"/>
                </Grid.ColumnDefinitions>

                <Button x:Name="BtnToggleBuffer" Content="{Binding BufferBtnText}" Background="{Binding BufferBtnBackground}" Grid.Column="0" Margin="2" Padding="8,5" FontWeight="Bold" Foreground="White" BorderThickness="0" Command="{Binding ToggleBufferCommand}" ToolTip="Toggle Buffer"/>
                <Button Content="END" Background="Orange" Grid.Column="1" Margin="2" Padding="8,5" FontWeight="Bold" Foreground="White" BorderThickness="0" Click="BtnEndSession_Click" ToolTip="Finish Season"/>
                <Button Background="#FF929292" Grid.Column="2" Margin="2" BorderThickness="0" Foreground="White"  Click="BtnOpenHistory_Click" ToolTip="User Hub">
                    <iconPacks:PackIconMaterial Kind="ViewList" Width="16" Height="16" />
                </Button>
                <Button Background="#FF929292" Grid.Column="4" Margin="2" BorderThickness="0" Foreground="White"  Click="BtnOpenHub_Click" ToolTip="User Hub">
                    <iconPacks:PackIconMaterial Kind="AccountCircle" Width="16" Height="16" />
                </Button>

                <Button Background="#FF929292" Grid.Column="3" Margin="2" BorderThickness="0" Foreground="White" Click="BtnOpenOverlay_Click" ToolTip="Open Overlay">
                    <iconPacks:PackIconMaterial Kind="Layers" Width="16" Height="16" />
                </Button>

                <Button Background="#FF929292" Grid.Column="5" Margin="2" BorderThickness="0" Foreground="White" Command="{Binding ToggleStatsCommand}" ToolTip="Toggle Stats">
                    <iconPacks:PackIconMaterial Kind="Clock" Width="16" Height="16" />
                </Button>

                <Button Background="#FF929292" Grid.Column="6" Margin="2" BorderThickness="0" Foreground="White" Click="BtnOpenSettings_Click" ToolTip="Settings">
                    <iconPacks:PackIconMaterial Kind="Cog" Width="16" Height="16" />
                </Button>
            </Grid>
        </Grid>
    </Border>
</Window>